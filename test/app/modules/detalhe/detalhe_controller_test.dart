import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:destinymenes/app/modules/detalhe/detalhe_controller.dart';
import 'package:destinymenes/app/modules/detalhe/detalhe_module.dart';

void main() {
  initModule(DetalheModule());
  // DetalheController detalhe;
  //
  setUp(() {
    //     detalhe = DetalheModule.to.get<DetalheController>();
  });

  group('DetalheController Test', () {
    //   test("First Test", () {
    //     expect(detalhe, isInstanceOf<DetalheController>());
    //   });

    //   test("Set Value", () {
    //     expect(detalhe.value, equals(0));
    //     detalhe.increment();
    //     expect(detalhe.value, equals(1));
    //   });
  });
}
